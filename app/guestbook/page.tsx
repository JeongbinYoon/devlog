'use client';

import { Scene } from '@/components/three';

const GuestbookPage = () => {
  return (
    <div style={{ width: '100vw', height: '100vh' }}>
      <Scene />
    </div>
  );
};
export default GuestbookPage;
